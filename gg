    CUSTOMER_NAME=$(gitlab-terraform output -raw name)
      REGION=$(gitlab-terraform output -raw playbook_region)

      # Debugging: Print CUSTOMER_NAME and REGION variables
      echo "CUSTOMER_NAME: $CUSTOMER_NAME"
      echo "REGION: $REGION"
